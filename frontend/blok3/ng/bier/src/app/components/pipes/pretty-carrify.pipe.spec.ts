import { PrettyCarrifyPipe } from './pretty-carrify.pipe';

describe('PrettyCarrifyPipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyCarrifyPipe();
    expect(pipe).toBeTruthy();
  });
});
